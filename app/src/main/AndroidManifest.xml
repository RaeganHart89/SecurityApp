<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.security" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.sitepoint.security.ToDoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="SecurityApp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.sitepoint.security.MainActivity"
            android:label="SecurityApp"
            android:theme="@style/AppTheme" >
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.sitepoint.security.LoginActivity" />
        <activity android:name="com.sitepoint.security.SignUpActivity" />
        <activity android:name="com.sitepoint.security.OptionsActivity" />
        <activity android:name="com.sitepoint.security.ClickCounter" />
        <activity android:name="com.sitepoint.security.BannedListActivity" />
        <activity android:name="com.sitepoint.security.RulesListActivity" />
        <activity android:name="com.sitepoint.security.MapActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCQjk-tzDi1hxb7eQrKvayirxpCvd7NDFI" />
    </application>

</manifest>